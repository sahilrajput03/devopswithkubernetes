apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ex1-01-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ex1-01-svc
                port:
                  number: 2345
